version: '3.3'

services:
  modelling_db:
    restart: on-failure
    image: mysql/mysql-server:8.0
    container_name: modelling-mlflow-db
    expose:
      - "3306"
    networks:
      - modelling_network
    environment:
      - MYSQL_DATABASE=${MO_MYSQL_DATABASE}
      - MYSQL_USER=${MO_MYSQL_USER}
      - MYSQL_PASSWORD=${MO_MYSQL_PASSWORD}
      - MYSQL_ROOT_HOST=${MO_MYSQL_ROOT_HOST}
      - MYSQL_ROOT_PASSWORD=${MO_MYSQL_ROOT_PASSWORD}
    volumes:
      - ${MO_DATA_PATH}/dbdata:/var/lib/mysql
  modelling:
    build:
      context: .
    image: modelling-image
    container_name: modelling-mlflow
    volumes:
      - ${MO_DATA_PATH}/mlruns:/code/mlruns
      - ${MO_DATA_PATH}/train_data:/train_data
      - ${MO_DATA_PATH}/test_data:/test_data
      - ${MO_DATA_PATH}/tmp_artifacts:/tmp_artifacts
      - ./modelling:/code/modelling
      - ./MLproject:/code/MLproject
      - ./conda.yaml:/code/conda.yaml
    networks:
      - modelling_network
    environment:
      - MLFLOW_TRACKING_URI=${MO_MLFLOW_TRACKING_URI}
    stdin_open: true
    tty: true
    depends_on:
      - modelling_db
  modelling_web:
    restart: on-failure
    build:
      context: .
    image: modelling-image
    container_name: modelling-mlflow-server
    ports:
      - "5001:5000"
    networks:
      - modelling_network
    environment:
      - MLFLOW_TRACKING_URI=${MO_MLFLOW_TRACKING_URI}
#    command: wait-for-it modelling-mlflow-db:3306 -s -- mlflow server --backend-store-uri $MLFLOW_TRACKING_URI --default-artifact-root ./mlruns --host 0.0.0.0
    stdin_open: true
    tty: true
    volumes:
      - ${MO_DATA_PATH}/mlruns:/code/mlruns
    depends_on:
      - modelling_db

networks:
  modelling_network:
    driver: bridge
